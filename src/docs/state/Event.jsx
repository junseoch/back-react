import React from 'react';

const Event = () => {

  return (
    <div>
      <input type="text" onChange={ () => {} } />
                          {/* // 앞에 있는 onChange를 받아서 처리하는 콜백함수 */}
    </div>
  );
};

export default Event;