// import React, { useState } from 'react';

// const LikesContainer = () => {

  
//   // 1. likeList를 화면에 출력한다.
//   // 2. 버튼을 누르면 해당 like를 삭제한다.
//   // 3. input 태그를 하나 생성 후 좋아하는 목록을 적고 엔터를 치면, 
//   // 리스트에 추가된다.


//   // 객체를 버튼으로 바꿔서 출력
//   // 리스트에서 map으로 
  


//   return (
//     <li>
//       {likeList}
//       <input type="text" />
//     </li>
//   );
// };

// export default LikesContainer;


//   // const studentList = attendanceData.map((student) => <li>{student.name}</li>)
//   const likeList = like.map((like,i) => <button key={i}>{like.name}</button>)
//   console.lo